{
  "name": "app-platform-recipes",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "manifest:build": "node scripts/build-manifest.mjs",
    "manifest:check": "node scripts/build-manifest.mjs check",
    "test": "node scripts/check-recipes.mjs && node scripts/build-manifest.mjs check"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
